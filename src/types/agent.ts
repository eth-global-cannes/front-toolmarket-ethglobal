export interface Agent {
  uuid: string;
  id: number;
  imageUrl: string;
  name: string;
  description: string;
  note: number;
  // votes: number;
  price?: string;
  // type?: string;
  // developer?: string;
  // releaseDate?: string;
  // category?: string;
  // longDescription?: string;
  // isPurchased?: boolean;
}
